name: image_upload
description: A Flutter project demonstrating image upload activity.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^0.8.0+3
  flutter_bloc: ^7.0.1
  equatable: ^2.0.3

  dio: ^4.0.0

  injectable: ^1.4.1
  get_it: ^7.1.4

  flutter_native_image: ^0.0.6

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  injectable_generator: ^1.4.1
  build_runner: ^2.0.4

dependency_overrides:
  async: ^2.7.0

flutter:
  uses-material-design: true